require('./src/accessor_patch.js');
yasw= require('./src/yasw_server.js');
yasw.createServer({
  tick_rate: 20,
  ship_rotation_rate: Math.PI,
  acceleration_rate: 20,
  bullet_speed: 65,
  top_edge: 1024,
  right_edge: 1024
}).listen(3000);
